if (confirm("Are you sure you want to spend time on YouTube Shorts?")) {
    // User confirmed, allow navigation
} else {
    // User canceled, redirect to a different page
    window.location.href = "about:blank";
}
